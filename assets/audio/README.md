# Audio assets

Place recorded MP3 files in these folders:

- `letters/` — letter names (`a.mp3`)
- `words/` — word names (`arbuz.mp3`)
- `effects/` — `correct.mp3`, `wrong.mp3`, `win.mp3`, `click.mp3`

Missing or unloadable files are ignored by the shared audio module.
